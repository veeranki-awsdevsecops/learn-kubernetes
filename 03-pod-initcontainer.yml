apiVersion: v1
kind: Pod
metadata:
  name: configmap-pod-1

spec:
  initContainers:
    - name: test1
      image: nginx:1.12.0

  containers:
    - name: test
      image: nginx:1.14.0
      volumeMounts:
        - name: config-vol
          mountPath: C:\Users\
  volumes:
    - name: config-vol
      emptyDir: {}
