apiVersion: v1
kind: Pod
metadata:
  name: configmap-pod

spec:
  containers:
    - name: test
      image: nginx:1.14.0
      volumeMounts:
        - name: config-vol
          mountPath: C:\Users\
  volumes:
    - name: config-vol
      emptyDir: {}
